{
  "name": "eon-xrp-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "core",
    "backend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.12.4"
  }
}
